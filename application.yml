spring:
  profiles:
    active:
    - dev
---
spring:
  profiles: dev
  application:
    name: microservicecloud-config-plpig-dev
---
spring:
  profiles: test
  application:
    name: microservicecloud-config-plpig-test
